function OctaveWindow(level) {
  var pX, pY, xPressed, yPressed, keyNote, octaveP, octNote, octPerWindow, startOctave;
  var whiteWidth = 50;
  var blackWidth = 30;
  var fequency = {};
  var octaves = [];
  var octaveWidth = 350;
}
